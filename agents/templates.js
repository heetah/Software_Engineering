// 測試輸出格式模板
export const TEST_OUTPUT_FORMAT = `## 輸出格式要求

必須使用以下格式輸出每個測試檔案：

<!-- file: tests/index.test.js -->
\`\`\`javascript
// 完整的測試檔案
const { describe, it, expect, beforeEach } = require('測試框架');
const 被測試模組 = require('../src/index');

describe('完整測試套件', () => {
  beforeEach(() => {
    // 設定
  });

  it('應該通過第一個測試', () => {
    // 完整的測試實作，包含斷言
    expect(true).toBe(true);
  });

  // ... 更多完整的測試案例
});
\`\`\`

<!-- file: tests/controllers/userController.test.js -->
\`\`\`javascript
// 完整的控制器測試
// ... 完整的測試實作
\`\`\``;

// 測試撰寫重要提示模板
export const TEST_IMPORTANT_TIPS = `## 重要提示

**關鍵要求：**
- **生成 3-5 個完整的測試檔案，涵蓋主要功能**
- **每個測試檔案必須完整，包含：**
  - 完整的 import/require 語句
  - 完整的測試框架設定（describe, it, beforeEach 等）
  - **每個測試案例都必須有完整的實作和斷言，不要只有註解**
  - 錯誤處理和邊界情況測試
- **測試必須是可執行的，不要只有 TODO 或註解**
- **必須測試所有主要功能模組：控制器、路由、模型、服務等**
- 每個檔案都必須用 <!-- file: 路徑 --> 標記
- 必須緊接三引號程式碼區塊
- **如果測試被截斷，將無法執行，請確保每個檔案都完整**

**測試覆蓋：**
- 單元測試：測試單個函數或類別
- 整合測試：測試模組之間的互動
- API 測試：測試 HTTP 端點

**輸出優先級：**
1. 先生成最重要的功能測試（如用戶認證、CRUD 操作）
2. 確保每個測試檔案都包含多個測試案例
3. 確保所有測試都有完整的實作，不要只有框架或註解`;

// Verifier 問詢輸出模板
export const VERIFIER_OUTPUT_TEMPLATE = `# 澄清問題清單

請基於輸入的規格/程式碼，輸出條列且可執行決策的澄清問題，並用 Markdown 分段：

## 關鍵不確定性（必答）
- 逐點列出阻塞實作/測試的前 5-10 個問題

## 測試與資料假設
- 需要的測試資料、邊界值、錯誤情境

## 環境與設定
- 必要的環境變數、外部服務、權限/金鑰

## 建議的後續動作
- 對應每個問題，提出最小可行的解決路徑（若無法回答，標記為待提供）

輸出只需 Markdown 內容，不要夾雜多餘說明。`;


